# Plan — Order Number, Promo Persistence, Email Discount Breakdown

Supersedes the original 3-phase plan (Auth & Session Fixes, Checkout Flow Overhaul, UI/UX Revamp — all completed and committed). Phases run in order — each phase's UNTIL must be true before starting the next.

Admin role assignment is handled directly in the database by the owner — no seed/bootstrap script for it in this plan.

---

## Phase A — Scalable Order Number (`TVA<seq>`)

GOAL: Every order gets a short display number in the form `TVA<sequence>` (e.g. `TVA100001`), generated by a real Postgres sequence. The UUID (`orders.id`) stays the primary key/FK target and stays in every URL — it is never shown to a human. Every human-facing surface (customer UI, email, admin portal) shows only the `TVA<seq>` number.

UNTIL: A newly created order gets a real `TVA<seq>` number from the DB sequence; it displays correctly on the confirmation page, account order-history list, order status page, admin table (list + ship dialog + toast), and the confirmation email; existing test orders got sane backfilled numbers; the Cashfree order note shows the same number.

TASKS:
- [x] [db/schema.ts](db/schema.ts) — added `orderSeq: serial("order_seq").notNull().unique()` to `orders`; [lib/format.ts](lib/format.ts) — added `formatOrderNumber(seq) => "TVA" + seq`
- [x] Migration ([drizzle/0008_charming_taskmaster.sql](drizzle/0008_charming_taskmaster.sql)) — adds the column, backfills existing rows chronologically by `created_at` starting at `TVA100001`, restarts the sequence past the backfilled max, then adds the unique constraint; applied directly against the dev DB (drizzle-kit's `db:migrate` hung against the Neon HTTP driver in this environment — statements applied via a one-off script instead, same net result)
- [x] [app/api/orders/route.ts](app/api/orders/route.ts) — returns `orderSeq` on insert; Cashfree `note` now uses `formatOrderNumber(order.orderSeq)` instead of the raw UUID
- [x] [app/(storefront)/orders/[id]/confirmation/page.tsx](app/(storefront)/orders/[id]/confirmation/page.tsx) + [components/storefront/order-confirmation.tsx](components/storefront/order-confirmation.tsx) — "Order ID" line now shows `formatOrderNumber(orderNumber)`, not the UUID
- [x] [app/(storefront)/orders/[id]/page.tsx](app/(storefront)/orders/[id]/page.tsx) — added an "Order TVA…" line (previously showed no identifier at all)
- [x] [app/(storefront)/account/orders/page.tsx](app/(storefront)/account/orders/page.tsx) — each row now leads with the order number
- [x] [app/admin/orders/page.tsx](app/admin/orders/page.tsx) + [components/admin/orders-table.tsx](components/admin/orders-table.tsx) — table cell, ship-dialog title, and success toast all show `formatOrderNumber(order.orderSeq)` instead of a truncated UUID
- [x] [lib/email/templates/order-confirmation-email.tsx](lib/email/templates/order-confirmation-email.tsx) + [app/api/webhooks/cashfree/route.ts](app/api/webhooks/cashfree/route.ts) — email's "Order ID" line now shows the formatted order number
- [x] Verified via the browser: `/orders/<uuid>` (order status page) renders "Order TVA100002" with no UUID visible anywhere in the page text; `npx tsc --noEmit` clean

---

## Phase B — Promo Code Persistence (Cart → Checkout), URL-driven for future ad links

GOAL: A promo applied on the cart page is still applied at checkout — no re-entry required — and a promo code carried in the URL (e.g. `/products?promo=WELCOME10`, for future ad campaigns) auto-applies and then persists the same way. Global promo state lives in a `zustand` store; `nuqs` handles the `?promo=` URL param.

UNTIL: Apply a promo on `/cart`, go to `/checkout` — it's still applied with the same discount, no re-entry. Visiting any storefront page with `?promo=<valid-code>` auto-applies it, and it's still applied at checkout. Completing the order clears cart + promo together. Removing the promo anywhere clears it everywhere.

TASKS:
- [x] Added `nuqs` and `zustand` dependencies (via pnpm — `npm install` errored out in this environment)
- [x] [lib/promo/promo-store.ts](lib/promo/promo-store.ts) (new) — zustand store + `persist` middleware for `AppliedPromo | null`
- [x] [components/providers.tsx](components/providers.tsx) — wrapped in `<NuqsAdapter>`, `PromoUrlSync` mounted inside `CartProvider`
- [x] [components/storefront/promo-url-sync.tsx](components/storefront/promo-url-sync.tsx) (new) — reads `?promo=` via nuqs, validates against the live cart subtotal, writes to the store
- [x] [components/storefront/promo-code-input.tsx](components/storefront/promo-code-input.tsx) — reads/writes the zustand store directly instead of local state + `onApplied` prop; also re-validates the applied promo whenever `cartTotal` changes, so a promo auto-applied before the cart had items (or one that's since expired) gets its discount amount corrected instead of staying stale
- [x] [app/(storefront)/cart/page.tsx](app/(storefront)/cart/page.tsx) + [app/(storefront)/checkout/page.tsx](app/(storefront)/checkout/page.tsx) — dropped local `useState<AppliedPromo | null>`, read from the store
- [x] [components/storefront/order-confirmation.tsx](components/storefront/order-confirmation.tsx) — clears the promo store alongside cart `clear()`
- [x] Verified end-to-end in the browser: visited `/products?promo=WELCOME10` with an empty cart (auto-applied at ₹0 discount), added an item, cart page re-validated to the correct -₹100 discount, signed in via OTP, and the Payment step showed `WELCOME10` already applied with the same discount — no re-entry. Test user/address/verification rows cleaned up afterward.

---

## Phase C — Discount Breakdown in Email (+ Cashfree note)

GOAL: The order confirmation email shows Subtotal, Discount (with promo code) when applicable, and Total. No shipping/tax rows — no such columns exist and adding them is explicitly out of scope for this pass.

UNTIL: A test order with a promo shows Subtotal/Discount(with code)/Total in the email; an order with no promo shows just Subtotal/Total; the Cashfree order note contains the same breakdown.

TASKS:
- [x] [lib/email/templates/order-confirmation-email.tsx](lib/email/templates/order-confirmation-email.tsx) — added `subtotal`/`discount`/`promoCode` props; renders Subtotal always, a green Discount line (with promo code) only when `discount > 0`, then Total
- [x] [app/api/webhooks/cashfree/route.ts](app/api/webhooks/cashfree/route.ts) — forwards `order.subtotal`/`order.discount`/`order.promoCode` into the email
- [x] [app/api/orders/route.ts](app/api/orders/route.ts) — Cashfree `note` now includes the same Subtotal/Discount/Total breakdown when a promo applies (e.g. `Tanvira order TVA100005 — Subtotal ₹1,999, Discount ₹100 (WELCOME10), Total ₹1,899`, 82 chars — well under Cashfree's `order_note` limit); just the order number otherwise
- [x] Verified by rendering the email template directly (both with and without a discount) — with-discount output correctly shows `Subtotal: ₹1999.00 / Discount (WELCOME10): -₹100.00 / Total: ₹1899.00`; no-discount output shows `Subtotal: ₹1999.00 / Total: ₹1999.00` with no empty discount line. `npx tsc --noEmit` clean, scratch verification files removed.

---

## Phase D — Fix Order-Lookup Bug + React Query Cleanup

GOAL: Fix the broken "Track your order" link (real 404 bug), then remove the duplicated fetch/error-handling and date-formatting code that the Phase A–C and react-query changes left behind, and settle on one convention for refreshing lists after a mutation.

UNTIL: `/orders/[id]` resolves correctly whether it's reached via the UUID (confirmation page's "Track your order" link) or the `TVA<seq>` link (account order-history); there is exactly one function that loads an order + timeline; date formatting and the fetch/JSON/error-throw pattern each exist in exactly one place; shipping an order updates the admin table without relying on a different refresh mechanism than the rest of the react-query mutations.

TASKS:
- [x] **Bug**: collapsed `getOrderWithTimeline`/`getOrderWithTimelineWithOrderSeq` into one function ([lib/orders/db.ts](lib/orders/db.ts), formerly `lib/orders.ts`) that detects whether it was handed a UUID or a `TVA<seq>` number and branches the lookup accordingly. [app/(storefront)/orders/[id]/page.tsx](app/(storefront)/orders/[id]/page.tsx) now calls the unified function, so `/orders/[id]` resolves whether it's reached via the confirmation page's raw-UUID link or account order-history's `TVA<seq>` link — no caller-side link changes needed.
- [x] [lib/format.ts](lib/format.ts) — added shared `formatDate(date)`; replaced the copy-pasted inline version in [components/admin/orders-table.tsx](components/admin/orders-table.tsx), [components/admin/users-table.tsx](components/admin/users-table.tsx), and [app/(storefront)/account/orders/page.tsx](app/(storefront)/account/orders/page.tsx).
- [x] [app/(storefront)/account/orders/page.tsx](app/(storefront)/account/orders/page.tsx) — order-history link now uses `formatOrderNumber(order.orderSeq)` instead of re-deriving the `TVA` string inline.
- [x] Added [lib/http.ts](lib/http.ts)'s `fetchJson<T>` (parses JSON, throws on `!res.ok` using the same `error.message` shape as `apiError`); [lib/addresses/queries.ts](lib/addresses/queries.ts), [lib/orders/queries.ts](lib/orders/queries.ts), and [lib/promo/queries.ts](lib/promo/queries.ts) all use it now — the promo file's missing `res.ok` check is fixed as a side effect.
- [x] [lib/orders/queries.ts](lib/orders/queries.ts) — decided and documented: order/user lists are server components with no react-query cache to invalidate, so `useCreateOrder`/`useShipOrder` intentionally leave refreshing to `router.refresh()` (as `orders-table.tsx` already does); `invalidateQueries` stays reserved for client-fetched lists like `useAddresses`. Noted in comments so it isn't flipped back and forth later.
- [x] [components/storefront/promo-url-sync.tsx](components/storefront/promo-url-sync.tsx) — removed the `eslint-disable-next-line react-hooks/exhaustive-deps`; the effect now reads `subtotal`/`applyPromo`/`setPromo` through a ref kept fresh by a second effect, so its own dependency array only lists what should actually re-trigger it.
- [x] Renamed the same-stem server DB modules into their matching directories: `lib/addresses.ts` → [lib/addresses/db.ts](lib/addresses/db.ts), `lib/orders.ts` → [lib/orders/db.ts](lib/orders/db.ts), `lib/promo.ts` → [lib/promo/db.ts](lib/promo/db.ts) (via `git mv`, preserving history); all six import sites updated.
- [x] [lib/promo/queries.ts](lib/promo/queries.ts) — decision recorded in a comment: `usePromoRevalidation`'s `useQuery`-over-POST usage is intentional (the call is a pure, idempotent read), the per-subtotal cache growth is an accepted tradeoff, and it should not be "fixed" into a mutation later — `useApplyPromoMutation` stays the one-shot path.
- [x] [sanity/tools/ship-order-tool.tsx](sanity/tools/ship-order-tool.tsx) — added a comment explaining the intentional duplication of the ship-order request (Sanity Studio's separate React root can't reach `QueryClientProvider`) and pointing at `useShipOrder` so both stay in sync if the request ever changes. Still hand-rolled by design — not a quick fix, just no longer an unexplained duplicate.

Verified: `npx tsc --noEmit` clean; `npx eslint` clean on every file touched in this phase (the one repo-wide lint error found, in `app/(storefront)/checkout/page.tsx`, predates this phase and is out of scope).
